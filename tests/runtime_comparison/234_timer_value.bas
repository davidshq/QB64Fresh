$CONSOLE:ONLY
' Runtime comparison: TIMER = seconds since midnight (approximate; range 0-86400).
' Exact value differs by run and runtime; document as approximate in DIFFERENCES.
PRINT "timer:"; TIMER
END
