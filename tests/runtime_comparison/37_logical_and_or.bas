$CONSOLE:ONLY
' Runtime comparison: AND, OR, NOT
DIM a AS LONG, b AS LONG
a = 1
b = 0
PRINT "1 and 0:"; a AND b
PRINT "1 or 0:"; a OR b
PRINT "not 0:"; NOT 0
PRINT "1 and 1:"; 1 AND 1
PRINT "0 or 1:"; 0 OR 1
END
